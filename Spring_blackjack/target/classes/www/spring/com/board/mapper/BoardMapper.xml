<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC 
"-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="www.spring.com.board.mapper.BoardMapper">
	
	<select id="getList" resultType="www.spring.com.board.model.BoardVO">
		<![CDATA[
			select * from qna_board where bno > 0
		]]>
	</select>
	
	<sql id="criteria">
		<trim prefix="(" suffix=") AND " prefixOverrides="OR">
				<foreach item="type" collection="typeArr">
					<trim prefix="OR">
						<choose>
							<when test="type == 'T'.toString()">
								title like '%'||#{keyword}||'%'
							</when>
							<when test="type == 'C'.toString()">
								content like '%'||#{keyword}||'%'
							</when>
							<when test="type == 'W'.toString()">
								writer like '%'||#{keyword}||'%'
							</when>
						</choose>
					</trim>
				</foreach>
			</trim>
	</sql>
	
	<select id="getListWithPaging" resultType="www.spring.com.board.model.BoardVO">
		<![CDATA[
			SELECT
			    bno, title, content, writer , regdate, updateDate, replycnt
			FROM
			    (
			    SELECT /*+INDEX_DESC(qna_board pk_qna_board) */
			        rownum rn, bno, title, content, writer , regdate, updateDate, replycnt
			    FROM 
			    	qna_board
			    where 
		]]>
			
		<include refid="criteria"></include>
			
		<![CDATA[
			rownum <= #{pageNum} * #{amount}
			)
		where rn > (#{pageNum} -1) * #{amount}
		]]>
	</select>
	
	<select id="getTotalCount" resultType="int">
	select count(*) from qna_board 
	where 
			
	<include refid="criteria"></include>			
			
	bno > 0
	</select>
	<!-- 체크 바람 -->
	
	<insert id="insert">
			<![CDATA[
				insert into qna_board (bno, title, content, writer)
				values (seq_qnaboard.nextval, #{title}, #{content}, #{writer})
			]]>
	</insert>
	
	<insert id="insertSelectKey">
		<!-- MyBatis의 어노테이션 PK값을 미리 SQL을 통해서 처리해 두고 특정한 이름으로 결과를 보관하는 방식 -->
		<selectKey keyProperty="bno" order="BEFORE"
			resultType="long">
			select seq_qnaboard.nextval from dual	
		</selectKey>
		
		insert into qna_board (bno, title, content, writer)
		values (#{bno}, #{title}, #{content}, #{writer})
	</insert>
	
	<select id="read" resultType="www.spring.com.board.model.BoardVO">
			<![CDATA[
				select * from qna_board where bno = #{bno}
			]]>
	</select>
				
	<delete id="delete">
		delete from qna_board where bno = #{bno}
	</delete>
	
	<update id="update">
		update qna_board
		set title = #{title},
		content = #{content},
		writer = #{writer},
		updateDate = sysdate
		where bno = #{bno}
	</update>
	
	<update id="updateReplyCnt">
		update qna_board set replycnt = replycnt + #{amount} where bno = #{bno}
	</update>

</mapper>